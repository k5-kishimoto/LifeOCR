[server]
# CORSを完全に無効化
enableCORS = false
# XSRF保護を無効化（埋め込みに必須）
enableXsrfProtection = false
# iframe内での動作を許可
frameFeatures = "none"
# 以下の1行を念のため追加（Streamlitの内部的なヘッダー制御を緩める）
enableStaticServing = true